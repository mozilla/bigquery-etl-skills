# Critical table monitoring configuration for metadata.yaml
# Use this template for production-critical tables where failures should block pipelines

monitoring:
  enabled: true
  freshness:
    enabled: true
    blocking: true
  volume:
    enabled: true
    blocking: true
  collection: "Operational Checks"
