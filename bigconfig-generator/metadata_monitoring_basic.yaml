# Basic monitoring configuration for metadata.yaml
# Use this template for standard daily updated tables

monitoring:
  enabled: true
  freshness:
    enabled: true
    blocking: false
  volume:
    enabled: true
    blocking: false
